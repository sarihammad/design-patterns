package behavioral.observer;

/**
 * Observer interface
 * This defines the interface for objects that should be notified of changes
 */
public interface Observer {
    void update(String message);
} 